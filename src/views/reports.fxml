<?xml version="1.0" encoding="UTF-8"?>

<?import java.lang.*?>
<?import java.util.*?>
<?import javafx.scene.*?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<AnchorPane xmlns="http://javafx.com/javafx"
            xmlns:fx="http://javafx.com/fxml"
            fx:controller="controllers.ReportsController"
            prefWidth="900" prefHeight="600"
            styleClass="page-pane">

    <VBox alignment="TOP_CENTER" spacing="20" prefWidth="780" layoutX="10" layoutY="10"
          styleClass="card">
        <Label text="📊 Sales Reports"
               style="-fx-font-size: 22px; -fx-font-weight: bold; -fx-text-fill: #ff9800;" />

        <HBox spacing="10" alignment="CENTER_LEFT">
            <Label text="View:" />
            <ChoiceBox fx:id="reportType" prefWidth="120" />
            <Button text="Generate" onAction="#handleGenerateReport"
                    styleClass="btn-blue" />
            <Button text="Export CSV" onAction="#handleExportCSV"
                    styleClass="btn-green"/>
            <Button text="Export PDF" onAction="#handleExportPDF"
                    styleClass="btn-red" />
        </HBox>

        <TableView fx:id="salesTable" prefHeight="350">
            <columns>
                <TableColumn fx:id="colId" text="Sale ID" prefWidth="80" />
                <TableColumn fx:id="colProduct" text="Product" prefWidth="200" />
                <TableColumn fx:id="colQuantity" text="Qty" prefWidth="80" />
                <TableColumn fx:id="colTotal" text="Total (₱)" prefWidth="100" />
                <TableColumn fx:id="colDate" text="Date" prefWidth="150" />
            </columns>
        </TableView>

        <HBox spacing="50" alignment="CENTER_RIGHT">
            <Label fx:id="totalSalesLabel"
                   text="Total Sales: ₱0.00"
                   style="-fx-font-size: 18px; -fx-font-weight: bold; -fx-text-fill: #4caf50;" />
            <Label fx:id="transactionCountLabel"
                   text="Transactions: 0"
                   style="-fx-font-size: 18px; -fx-font-weight: bold; -fx-text-fill: #0078ff;" />
        </HBox>
    </VBox>
</AnchorPane>
